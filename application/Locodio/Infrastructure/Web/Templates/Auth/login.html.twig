{# #}
{# This file is part of the Locod.io software. #}
{# #}
{# (c) Koen Caerels #}
{# #}
{# For the full copyright and license information, please view the LICENSE #}
{# file that was distributed with this source code. #}
{# #}

{% trans_default_domain 'auth' %}
{% extends 'base-dark.html.twig' %}

{% block title %} Locod.io: Please login{% endblock %}

{% block body %}
    <div style="position: absolute; top:5%;left:0;right:0">
        <div>
            <div>
                <div style="max-width: 450px;" class="mx-auto">
                    <div class="bg-white mt-12 p-8 drop-shadow-xl">
                        {# header ----------------------------------------------------------------------------------- #}
                        <div class="text-center">
                            <div class="mb-2">
                                <img src="{{ logo }}" width="100" class="mx-auto">
                            </div>
                        </div>
                        <hr class="mt-4">

                        {# messages --------------------------------------------------------------------------------- #}
                        {% if error %}
                            <div class="mt-2 mb-2 bg-yellow-100 rounded-lg p-2 text-center text-sm">
                                {{ error.messageKey|trans(error.messageData, 'security') }}
                            </div>
                            <hr class="mt-2">
                        {% endif %}

                        {% for message in app.flashes('success') %}
                            <div class="mt-2 mb-2 bg-green-300 rounded-lg p-2 text-center text-sm">
                                {{ message | trans }}
                            </div>
                            <hr class="mt-2">
                        {% endfor %}


                        {# form ------------------------------------------------------------------------------------- #}
                        <form action="{{ path('app_login') }}" method="post">

                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"/>
                            <input type="hidden" name="_target_path" value="{{ path('locodio_app_index') }}"/>

                            <div class="mt-2">
                                <label for="username">Email</label>
                            </div>
                            <div class="mt-2">
                                <input type="email"
                                       id="username"
                                       name="_username"
                                       value="{{ last_username }}"
                                       required
                                       class="border-[1px] border-gray-300 p-2 rounded-lg w-full"
                                />
                            </div>
                            <div class="mt-2">
                                <label for="password">Password</label>
                            </div>
                            <div class="mt-2">
                                <input type="password"
                                       id="password"
                                       name="_password"
                                       required
                                       class="border-[1px] border-gray-300 p-2 rounded-lg w-full"
                                />
                            </div>

                            <div class="mt-4">
                                <button type="submit"
                                        class="bg-indigo-500 w-full text-white rounded-lg uppercase p-2 hover:bg-indigo-700"
                                >
                                    Sign In
                                </button>
                            </div>

                            <div class="mt-2">
                                <label>
                                    <input type="checkbox" name="_remember_me" checked/>
                                    Keep me signed in
                                </label>
                            </div>

                            {# footer ------------------------------------------------------------------------------- #}
                            <div class="mt-8 text-gray-400 text-sm flex">
                                <div>
                                    <a href="{{ path('app_forgot') }}" class="text-sm text-indigo-800 underline underline-offset-4">
                                        Forgot&nbsp;password?
                                    </a>
                                </div>
                                <div class="ml-8 w-full text-right">
                                    {% if app_has_registration == 'true' %}
                                    New to locod.io?
                                    <a href="{{ path('app_sign_up') }}" class="text-sm text-indigo-800 underline underline-offset-4">
                                        Sign up here.
                                    </a>
                                    {% endif %}
                                </div>
                            </div>

                            {# SSO providers ------------------------------------------------------------------------ #}
                            <div class="mt-8 flex">
                                {% for provider in sso_providers %}
                                    <div class="mb-4 text-center mx-auto">
                                        <a href="{{ path('app_login_sso', {'providerName': provider.provider|lower}) }}"
                                           class="text-sm text-indigo-800 underline underline-offset-4">
                                            Sign in with {{ provider.provider }}
                                        </a>
                                    </div>
                                {% endfor %}
                            </div>

                        </form>
                    </div>
                </div>
            </div>

        </div>
    </div>

{% endblock %}
