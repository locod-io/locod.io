framework:
    default_locale: en
    translator:
        default_path: '%kernel.project_dir%/application/Locodio/Infrastructure/Translations'
        fallbacks:
            - en
#        providers:
#            crowdin:
#                dsn: '%env(CROWDIN_DSN)%'
#            loco:
#                dsn: '%env(LOCO_DSN)%'
#            lokalise:
#                dsn: '%env(LOKALISE_DSN)%'
